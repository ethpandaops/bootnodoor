
name: Build Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version Number ('0.x.y')"
        required: true

permissions:
  contents: write

jobs:
  build_binaries:
    name: "Build bootnodoor"
    uses: ./.github/workflows/_shared-build.yaml
    with:
      ref: ${{ github.sha }}
      release: "v${{ inputs.version }}"
      docker: true
      docker_repository: "ethpandaops/bootnodoor"
      docker_tag_prefix: "v${{ inputs.version }}"
      additional_tags: "['v${{ inputs.version }}','latest']"
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
  
  create_release:
    name: Create Release
    needs: [build_binaries]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        fetch-depth: 100
        ref: ${{ github.sha }}
    - name: "Generate release changelog"
      id: changelog
      run: |
        git fetch --tags
        prev_tag=$(git tag --sort=-version:refname | grep -e "^v[0-9.]*$" | head -n 1)
        echo "previous release: $prev_tag"
        touch ./generated_changelog.txt
        if [ "$prev_tag" ]; then
          changelog=$(git log --oneline --no-decorate $prev_tag..HEAD)
        else
          changelog=$(git log --oneline --no-decorate)
        fi
        echo "changelog<<EOF" >> $GITHUB_OUTPUT
        echo " - ${changelog//$'\n'/$'\n' - }" >> $GITHUB_OUTPUT
        echo " - ${changelog//$'\n'/$'\n' - }" >> ./generated_changelog.txt
        echo "EOF" >> $GITHUB_OUTPUT

    - name: "Generate AI release notes"
      id: ai_release_notes
      run: |
        ./.github/scripts/ai-release-notes.sh "$(cat ./generated_changelog.txt)"
      env:
        OPENROUTER_MODEL: ${{ vars.OPENROUTER_MODEL }}
        OPENROUTER_TOKEN: ${{ secrets.OPENROUTER_TOKEN }}
        GITHUB_TOKEN: ${{ github.token }}
        GITHUB_REPOSITORY: ${{ github.repository }}

    # download build artifacts
    - name: "Download build artifacts"
      uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0

    # generate release artifacts
    - name: "Generate release package: bootnodoor_${{ inputs.version }}_windows_amd64.zip"
      run: |
        cd bootnodoor_windows_amd64
        zip -r -q ../bootnodoor_${{ inputs.version }}_windows_amd64.zip .
    - name: "Generate release package: bootnodoor_${{ inputs.version }}_linux_amd64.tar.gz"
      run: |
        cd bootnodoor_linux_amd64
        tar -czf ../bootnodoor_${{ inputs.version }}_linux_amd64.tar.gz .
    - name: "Generate release package: bootnodoor_${{ inputs.version }}_linux_arm64.tar.gz"
      run: |
        cd bootnodoor_linux_arm64
        tar -czf ../bootnodoor_${{ inputs.version }}_linux_arm64.tar.gz .
    - name: "Generate release package: bootnodoor_${{ inputs.version }}_darwin_arm64.tar.gz"
      run: |
        cd bootnodoor_darwin_arm64
        tar -czf ../bootnodoor_${{ inputs.version }}_darwin_arm64.tar.gz .

    # create draft release
    - name: Create latest release
      uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2.6.1
      with:
        draft: true
        prerelease: false
        name: "v${{ inputs.version }}"
        tag_name: "v${{ inputs.version }}"
        body: |
          ### Major Changes
          ${{ steps.ai_release_notes.outputs.release_notes }}

          <details>
            <summary>Full changelog</summary>

            ### Full changelog
          ${{ steps.changelog.outputs.changelog }}
          </details>

          ### Release Artifacts
          | Release File  | Description |
          | ------------- | ------------- |
          | [bootnodoor_${{ inputs.version }}_windows_amd64.zip](https://github.com/ethpandaops/bootnodoor/releases/download/v${{ inputs.version }}/bootnodoor_${{ inputs.version }}_windows_amd64.zip) | bootnodoor executables for windows/amd64 |
          | [bootnodoor_${{ inputs.version }}_linux_amd64.tar.gz](https://github.com/ethpandaops/bootnodoor/releases/download/v${{ inputs.version }}/bootnodoor_${{ inputs.version }}_linux_amd64.tar.gz) | bootnodoor executables for linux/amd64 |
          | [bootnodoor_${{ inputs.version }}_linux_arm64.tar.gz](https://github.com/ethpandaops/bootnodoor/releases/download/v${{ inputs.version }}/bootnodoor_${{ inputs.version }}_linux_arm64.tar.gz) | bootnodoor executables for linux/arm64 |
          | [bootnodoor_${{ inputs.version }}_darwin_arm64.tar.gz](https://github.com/ethpandaops/bootnodoor/releases/download/v${{ inputs.version }}/bootnodoor_${{ inputs.version }}_darwin_arm64.tar.gz) | bootnodoor executable for macos/arm64 |
        files: |
          bootnodoor_${{ inputs.version }}_windows_amd64.zip
          bootnodoor_${{ inputs.version }}_linux_amd64.tar.gz
          bootnodoor_${{ inputs.version }}_linux_arm64.tar.gz
          bootnodoor_${{ inputs.version }}_darwin_arm64.tar.gz
